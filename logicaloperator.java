package package1;

public class logicaloperator 
{
          public static void main(String[] args)
          {
        	  int a=100;
        	  int b=200;
        	  
        	  
        	  if(!(a==100 && a<b)) //not and operator
        		                // and condition is true so the not opeartor become false and the program will not execute
              {
        		  System.out.println(" not executed");
        	  }
          }
}
